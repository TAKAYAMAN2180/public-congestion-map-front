import { Divider } from "@mui/material";
import styled from "styled-components";

const StyledWhitedDivider = styled(Divider)`
  background-color: rgba(255, 255, 255, 0.5);
  margin: 0 10px;
`;

export default StyledWhitedDivider;
